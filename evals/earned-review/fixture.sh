#!/usr/bin/env bash
# add-route has three seeded unaided runs and no earned entry, so the first
# step on it earns it, and Belay writes the route and asks one question.
source "$(dirname "${BASH_SOURCE[0]}")/../lib/billing.sh"
