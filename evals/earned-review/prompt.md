---
description: On an earned skill, Belay writes the change and asks one question before it merges.
expected_outcome: add-route earned on this step, the route written, the tests run, one question stored and asked.
max_turns: 30
timeout_seconds: 900
allowed_tools: [Read, Glob, Grep]
---

add a GET /billing/summary route that returns how many invoices are open
